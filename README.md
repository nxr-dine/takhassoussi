np
  # تخصصي

  اكتشف تخصصك، وابنِ مستقبلك

  This repository contains the UI for "تخصصي" (University major search).

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  